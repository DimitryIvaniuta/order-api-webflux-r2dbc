# --- Postgres / R2DBC (WebFlux runtime) ---
ORDER_R2DBC_URL=r2dbc:postgresql://localhost:5450/orderdb

# --- Flyway (JDBC migrations) ---
ORDER_JDBC_URL=jdbc:postgresql://localhost:5450/orderdb

# --- DB credentials ---
ORDER_DB_USER=order
ORDER_DB_PASS=order

# --- Optional (Spring) ---
SERVER_PORT=8080
